from setuptools import setup

setup(name='dstbtn_test',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['dstbtn_test'],
      author= 'William Geuns',
      zip_safe=False)